package io.github.droidkaigi.confsched2023

import org.junit.Assert.assertEquals
import org.junit.Test

/**
 * Workaround for failing with No tests found
 */
class ExampleUnitTest {
    @Test
    fun addition_isCorrect() {
        assertEquals(4, 2 + 2)
    }
}
